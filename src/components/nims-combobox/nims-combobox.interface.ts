// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface INimsCombobox {
  required: boolean;
  disabled: boolean;
  compact: boolean;
  name: string;
  helperText: string;
  errorMessage: string;
  value: string;
  items: string[];
}
